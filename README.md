# Project illustrating binding conveyance in Clojure

Run the tests with

``` shell
clojure -X:run
```

Or an individual test (`com.github.bentomi.demo-test/multi-thread-conveying`)
with

``` shell
clojure -X:run :vars '[com.github.bentomi.demo-test/multi-thread-conveying]'
```
